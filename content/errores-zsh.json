{
    "title": "Resolviendo los 3 errores más frustrantes de ZSH",
    "tags": [
        "linux",
        "bash"
    ],
    "date": "22 Julio, 2026",
    "excerpt": "command not found: compdef, corrupt history file y más. Aprende a arreglar los problemas más comunes de ZSH.",
    "filename": "errores-zsh.html",
    "content": "<blockquote>\n                    <p>ZSH es increíblemente poderoso y personalizable, especialmente cuando lo combinas con frameworks como Oh My Zsh. Sin embargo, su configuración (el famoso archivo <code>.zshrc</code>) a veces puede romperse o generar mensajes de error extraños al abrir la terminal. Hoy te enseño cómo solucionar los tres dolores de cabeza más comunes de ZSH.</p>\n                </blockquote>\n\n                <h3 style=\"color: var(--accent-color); margin-top: 30px;\">1. El infame: \"command not found: compdef\"</h3>\n                <p>Este es probablemente el error más común para quienes instalan plugins manualmente o añaden autocompletados (como los de nvm, docker o git) a su <code>.zshrc</code>.</p>\n                <p><strong>¿Por qué pasa?</strong> El sistema de autocompletado de ZSH (<code>compinit</code>) no se ha cargado antes de que un plugin intente usar la función <code>compdef</code>.</p>\n                <p><strong>La solución:</strong> Debes asegurarte de inicializar el sistema de autocompletado <em>antes</em> de cargar tus plugins que dependan de él. Abre tu <code>~/.zshrc</code> y añade estas líneas justo antes de donde te da el error (o al inicio del bloque de plugins):</p>\n                <pre><code>autoload -Uz compinit\ncompinit</code></pre>\n\n                <h3 style=\"color: var(--accent-color); margin-top: 30px;\">2. Historial corrupto: \"zsh: corrupt history file\"</h3>\n                <p>Abres tu terminal y de repente ZSH se niega a guardar comandos nuevos o te lanza este error. Ocurre cuando la terminal se cierra de golpe o hay problemas de escritura en disco.</p>\n                <p><strong>¿Por qué pasa?</strong> El archivo <code>~/.zsh_history</code> tiene caracteres nulos o malformados y ZSH no puede leerlo.</p>\n                <p><strong>La solución:</strong> Tienes que extraer las partes legibles del historial y crear un archivo nuevo. Ejecuta estos comandos línea por línea:</p>\n                <pre><code>cd ~\nmv .zsh_history .zsh_history_bad\nstrings .zsh_history_bad > .zsh_history\nfc -R .zsh_history</code></pre>\n                <p>El comando <code>strings</code> extrae solo el texto legible del archivo dañado y reconstruye un historial sano. ¡Magia pura!</p>\n\n                <h3 style=\"color: var(--accent-color); margin-top: 30px;\">3. Errores de permisos con insecure directories</h3>\n                <p>A veces ves un bloque gigante de texto diciendo: <em>\"zsh compinit: insecure directories, run compaudit for list\"</em>.</p>\n                <p><strong>¿Por qué pasa?</strong> ZSH es muy estricto con la seguridad. Si carpetas como <code>/usr/local/share/zsh/site-functions</code> tienen permisos muy abiertos (ej. 777), ZSH se negará a cargar autocompletados desde allí para prevenir inyecciones de código.</p>\n                <p><strong>La solución:</strong> Tienes que corregir los permisos de las carpetas de autocompletado. Ejecuta:</p>\n                <pre><code>chmod -R 755 /usr/local/share/zsh/site-functions\nchown -R root:root /usr/local/share/zsh/site-functions</code></pre>\n                <p><em>Nota: Si usas macOS y Homebrew, podrías necesitar cambiar el propietario a tu usuario (<code>chown -R $(whoami) ...</code>).</em></p>\n\n                <h3 style=\"color: var(--accent-color); margin-top: 30px;\">Conclusión</h3>\n                <p>ZSH es una herramienta que requiere un poco de cariño. La próxima vez que veas un error en la terminal, no lo ignores con un <code>clear</code>. Entender estos mensajes te ayuda a conocer más a fondo tu sistema Linux o macOS.</p>"
}